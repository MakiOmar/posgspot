/**
 * qwik-speak configuration shim — messages live in src/i18n/*.json
 * and are loaded via ~/lib/i18n/translate.ts + I18nProvider.
 */
export { DEFAULT_CONTENT_LOCALE, STORE_LOCALES } from "./lib/i18n/config";
